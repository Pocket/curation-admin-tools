import { makeStyles, createStyles, Theme } from '@material-ui/core/styles';

/**
 * Styles for the FileUploadInfo component.
 */
export const useStyles = makeStyles((theme: Theme) =>
  createStyles({
    p: {
      margin: '0.25rem',
    },
  })
);
