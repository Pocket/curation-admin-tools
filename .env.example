# OAuth settings for local development.
# Auth uses the shared Mozilla SSO (production Cognito pool). This is safe
# because authentication is separate from the data API, so writes go to
# whichever API endpoint is configured below (dev by default).
REACT_APP_OAUTH2_REDIRECT_URI=http://localhost:3000/oauth/callback
REACT_APP_OAUTH2_CLIENT_ID=43e23kdco67rij2dmtg5bgqs53

# Uncomment to override the Admin API endpoint (defaults to https://admin-api.getpocket.dev).
# REACT_APP_ADMIN_API_ENDPOINT=http://localhost:4027
