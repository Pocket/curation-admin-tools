{
  "name": "collections-api",
  "version": "1.0.0",
  "main": "dist/src/main.js",
  "types": "src/main.ts",
  "private": true,
  "scripts": {
    "get": "cdktf get",
    "build:dev": "npm run build && NODE_ENV=development npm run synth",
    "build": "rm -rf dist && tsc",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w",
    "test": "echo ok",
    "upgrade": "npm i cdktf@latest cdktf-cli@latest",
    "upgrade:next": "npm i cdktf@next cdktf-cli@next"
  },
  "engines": {
    "node": "=12"
  },
  "dependencies": {
    "@cdktf/provider-aws": "1.0.130",
    "@cdktf/provider-null": "0.1.132",
    "@pocket/terraform-modules": "1.23.0",
    "cdktf": "0.4.1",
    "constructs": "3.3.111"
  },
  "devDependencies": {
    "@types/node": "^12.20.18",
    "cdktf-cli": "0.5.0",
    "typescript": "4.3.5"
  }
}
