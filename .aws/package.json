{
  "name": "curation-admin-tools",
  "version": "1.0.0",
  "main": "dist/main.js",
  "types": "src/main.ts",
  "private": true,
  "scripts": {
    "build:dev": "rm -rf dist && NODE_ENV=development npm run synth",
    "synth": "cdktf synth",
    "compile": "tsc --pretty",
    "watch": "tsc -w"
  },
  "engines": {
    "node": ">=12"
  },
  "dependencies": {
    "@pocket-tools/terraform-modules": "^3.1.15"
  },
  "devDependencies": {
    "@types/node": "^12.20.41",
    "typescript": "4.5.4"
  }
}
