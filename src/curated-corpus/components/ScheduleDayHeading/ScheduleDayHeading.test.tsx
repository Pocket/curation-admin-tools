// describe('The ScheduleDayHeading component', () => {
//   const onAddItem = jest.fn();
//   const setFilters = jest.fn();
//   const data: ScheduledCorpusItemsResult = []
//
//   it('renders successfully', () => {
//     render(<ScheduleDayHeading onAddItem={onAddItem} data={data} setFilters={setFilters}/>);
//
//     // there is at least a form and nothing falls over
//     const form = screen.getByRole('form');
//     expect(form).toBeInTheDocument();
//   });
//
// )};
